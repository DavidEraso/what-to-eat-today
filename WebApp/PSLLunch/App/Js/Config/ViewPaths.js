define(function () {
    var viewRootPath = 'Views/';

    var viewPaths = {

        main: viewRootPath + 'Main',
        config: viewRootPath + 'Config',
        pedido: viewRootPath + 'Pedido'

    };

    return viewPaths;
});
